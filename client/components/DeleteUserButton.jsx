import React from 'react';

const DeleteUserButton = ({
  data
}) => {
  
  return (
    <div id="DeleteUserButton">
        <button>Delete Current "Me"</button>
    </div>
  );
};

export default DeleteUserButton;
